<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pivot.aham.api.service.mapper.ModelRecommendBackUpMapper">

    <resultMap id="modelRecommendRes" type="com.pivot.aham.api.service.mapper.model.ModelRecommendBackUp">
        <result property="id" column="id"/>
        <result property="modelTime" column="model_time"/>
        <result property="risk" column="risk_level"/>
        <result property="age" column="age_level"/>
        <result property="modelStatus" column="model_status"/>
        <result property="score" column="score"/>
        <result property="currency" column="currency"/>
        <result property="productWeight" column="product_weight"/>
        <result property="classfiyWeight" column="classfiy_weight"/>
        <result property="portfolioId" column="portfolio_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="Columns">
        id,
        model_time,
        risk_level,
        age_level,
        model_status,
        score,
        currency,
        product_weight,
        classfiy_weight,
        portfolio_id,
        create_time,
        update_time
    </sql>

    <select id="queryModelRecommendBackUp" resultMap="modelRecommendRes">
        SELECT
        <include refid="Columns"/>
        FROM t_model_recommend_back_up
        <where>
            <if test="@Ognl@isNotEmpty(portfolioId)">
                and portfolio_id=#{portfolioId}
            </if>
            <if test="modelStatus != null">
                and model_status=#{modelStatus}
            </if>
        </where>
    </select>

</mapper>
